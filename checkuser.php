<html>
<head>
</head>
<body>
<?php

	// $id_token = $_GET["id_token"];
	// $access_token = $_GET["access_token"];

	// echo "<h1>id token .$id_token. , access token" .$access_token. "</h1>";

	// $id_curl = curl_init("https://www.googleapis.com/oauth2/v1/tokeninfo?alt=json&id_token={$id_token}");

	// $access_curl = curl_init();

	// curl_setopt_array($id_curl, array(
	// 	CURLOPT_RETURNTRANSFER => 1
	// 	CURLOPT_POST => 1,
	// 	CURLOPT_POSTFIELDS => array( 
	// 		alt => 'json', 
	// 		id_token => $id_token
	// 	)
	// ));

	// $arg1 = array(
	// 	CURLOPT_RETURNTRANSFER => true
	// );
//	curl_setopt_array($id_token, $arg1);

	// curl_setopt($id_curl, CURLOPT_RETURNTRANSFER, 1);

//	curl_setopt_array($access_curl, $arg1);

	// curl_setopt($access_curl, CURLOPT_URL, 'https://www.googleapis.com/oauth2/v1/userinfo?alt=json&access_token='.$access_token);
	// curl_setopt($access_curl, CURLOPT_RETURNTRANSFER, 1);
	// curl_setopt($access_curl, CURLOPT_USERAGENT, $_SERVER["HTTP_USER_AGENT"]);

	// $id_resp = curl_exec($id_curl);
	// $access_resp = curl_exec($access_curl);
	// echo "<h1> Here ".json_decode($access_resp)."</h1>";

	// $resp = [$id_resp, $access_resp];

	// $resp[0] = $id_resp;
	// $resp[1] = $access_resp;
	//	echo "<p>{$id_resp}</p>"
	// print(json_encode($resp));

	// $userDetail = file_get_contents("https://www.googleapis.com/oauth2/v1/userinfo?alt=json&access_token={$access_token}filename");
	// echo "<h1>{$userDetail}</h1>";

	// curl_close($access_curl);


	// $auth_code = curl_init("https://accounts.google.com/o/oauth2/auth");
	// $arg1 = array(
	// 	CURLOPT_RETURNTRANSFER => 1,
	// 	CURLOPT_POST => 1,
	// 	CURLOPT_POSTFIELDS => array(
	// 		scope => $email,
			
	// 	)
	// );

?>
</body>
</html>